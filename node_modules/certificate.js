'use strict'

const fs = require('fs')
const path = require('path')

module.exports = {
    key: fs.readFileSync(path.join(__dirname, 'tutorial.key')),
    cert: fs.readFileSync(path.join(__dirname, 'tutorial.cert'))
}
